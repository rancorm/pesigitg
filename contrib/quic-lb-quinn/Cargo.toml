[package]
name = "quic-lb-quinn"
description = "Quinn ConnectionIdGenerator binding for QUIC-LB"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
# Encoder core (no Quinn dep, suitable for any QUIC stack).
quic-lb-core = { workspace = true }

# Quinn trait this crate implements.
quinn = { workspace = true }
quinn-proto = { workspace = true }
